# Implementation Discovery Notes: FlexLM License Import

## Technology and Repository Context

- The repository is Go 1.27. The root `goflexlm` module is a streaming FlexNet debug-log parser and JSONL command. The requested work adds a license-file parser in the root package and extends the in-progress SQLite analytics command.
- SQLite uses `database/sql` and `modernc.org/sqlite`; `go.mod` is the only module definition. This is an existing exception to the root standard-library-only policy, not a new dependency proposed by the change.
- Root [AGENTS.md](../../../AGENTS.md) governs this work. It requires Go formatting, useful errors, no parser panics, bounded parser memory, and preservation of public command/API contracts unless an explicit compatibility decision says otherwise.
- No `CLAUDE.md`, `project-discovery.md`, project `.han/config.md`, ADR under `docs/adr/` or `docs/architecture/decisions/`, or separate coding-standard file was found. The root `AGENTS.md` is the applicable coding standard.

## Source Specification and Boundary

- The source is [change-plan.md](../change-plan.md), a complete feature-equivalent plan produced by `plan-a-change`, with supporting current-state, decision, review-finding, and review-history artifacts.
- The boundary is [scope-boundary.md](scope-boundary.md): replace the existing CSV entitlement workflow with direct FlexLM license-file parsing and import; preserve the debug-log parser; accept clean schema initialization instead of migration. No visual material was received.
- The source has no `feature-technical-notes.md`; no T# mechanics apply to this run.

## Code Touch Points

- `types.go`, new `license.go`, and new `license_parser.go`: the root public license document and `ParseLicenseFile(io.Reader)` belong beside the existing parser API, without SQLite dependencies.
- `sqlite/types.go`, `sqlite/entitlements.go`, `sqlite/schema.go`, and `sqlite/store.go`: replace the public CSV-entitlement API and version-1 entitlement table with the validated document snapshot import, version-2 initialization schema, and atomic capacity projection.
- `sqlite/reports.go`: replace feature-only capacity lookup with vendor-plus-feature capacity changes and expose counted, uncounted, and missing states correctly.
- `cmd/goflexlmdb/main.go` and its tests: replace `entitlements replace` with `licenses parse` and `licenses import`, preserving established status/error behavior for the existing commands.
- `README.md` and `testdata/sqlite-demo/`: replace the CSV-facing documentation and demo material with reproducible license-file examples.
- Existing tests are colocated: `decoder_test.go`, `sqlite/store_test.go`, `sqlite/derive_test.go`, and `cmd/goflexlmdb/main_test.go`. The source plan calls for additional focused parser, store, report, and command tests.

## Recent Activity and Working-Tree Context

- `git log --since="90 days ago" --name-only --pretty=format:"" -- sqlite cmd/goflexlmdb testdata/sqlite-demo` returned zero distinct paths. The SQLite and database-command implementation is uncommitted working-tree content, so it is current-state evidence but not committed precedent.
- The working tree is already dirty: `README.md` and `go.mod` are modified; `DESIGN.md`, `cmd/goflexlmdb/`, `docs/`, `go.sum`, `sqlite/`, and `testdata/` are untracked. Planning must preserve those user-owned changes and make no implementation edits.

## Measurements

- Measurement: existing SQLite package files | `find sqlite -maxdepth 1 -type f | wc -l` | 9.
- Measurement: existing database-command files | `find cmd/goflexlmdb -maxdepth 1 -type f | wc -l` | 2.
- Measurement: existing SQLite demo files | `find testdata/sqlite-demo -maxdepth 1 -type f | wc -l` | 7.
- Measurement: scoped committed churn in the last 90 days | `git log --since="90 days ago" --name-only --pretty=format:"" -- sqlite cmd/goflexlmdb testdata/sqlite-demo | sed '/^$/d' | sort -u | wc -l` | 0.
- Measurement: source plan’s schema-version-2, two-command, and two-work-unit assertions | static design commitments, not a repository quantity | not independently measured.

## Explicit Gaps

- No production license file, production database, or external consumer was available for inspection; the source plan already records its concrete reopening triggers for vendor-specific behavior.
- No ADR or established implementation-plan precedent exists in the repository.
- No visual design or UI flow is in scope.
